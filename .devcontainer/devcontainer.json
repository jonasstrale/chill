{
  "name": "chillplatform",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "remoteUser": "node",
  "features": {},
  "containerEnv": {
    "COREPACK_ENABLE_DOWNLOAD_PROMPT": "0"
  },
  "postCreateCommand": "corepack enable && pnpm install && pnpm --filter @economy/e2e exec playwright install chromium",
  "remoteEnv": {
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}",
    "COREPACK_ENABLE_DOWNLOAD_PROMPT": "0"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "OpenAI.chatgpt",
        "dbaeumer.vscode-eslint",
        "ms-azuretools.vscode-docker",
        "eamodio.gitlens",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "github.copilot.enable": {
          "*": false,
          "plaintext": false,
          "markdown": false,
          "scminput": false
        }
      }
    }
  },
  "postStartCommand": "corepack enable"
}
